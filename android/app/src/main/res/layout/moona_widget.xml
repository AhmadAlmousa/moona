<?xml version="1.0" encoding="utf-8"?>
<!-- Moona home-screen widget: header (title + detail toggle + open + add) over
     the list of items. Rendered entirely by MoonaWidgetProvider, which adds the
     item rows directly into widget_list_container. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="9dp">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/moona_on_surface"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <ImageView
            android:id="@+id/widget_detail_toggle"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:padding="7dp"
            android:background="@drawable/widget_chip_bg"
            android:src="@drawable/ic_widget_detail"
            android:contentDescription="@string/widget_description" />

        <ImageView
            android:id="@+id/widget_open"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="8dp"
            android:padding="7dp"
            android:background="@drawable/widget_chip_bg"
            android:src="@drawable/ic_widget_open"
            android:contentDescription="@string/widget_open_app" />

        <ImageView
            android:id="@+id/widget_add"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="8dp"
            android:padding="7dp"
            android:background="@drawable/widget_add_bg"
            android:src="@drawable/ic_widget_add"
            android:contentDescription="@string/widget_label" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:padding="16dp"
        android:textColor="@color/moona_on_surface_variant"
        android:textSize="14sp" />

    <!-- Item rows are added directly here by MoonaWidgetProvider (no ListView /
         RemoteViewsService, which third-party launchers fail to host). -->
    <LinearLayout
        android:id="@+id/widget_list_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical" />
</LinearLayout>
